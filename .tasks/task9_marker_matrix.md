# Persona

Раніше було проаналізовано якість розпізнавання (Detection Rate) ArUco-маркери.
Результати розпізнавання - utils\marker_bit_matrix.txt

Пізніше було запропоновано перелік гіпотез щодо властивостей бітових матриць маркерів, 
які найбільш явно корелюють з Detection Rate - .tasks\marker_matrix_info.md

# Tasks

1) Проаналізуй перелік гіпотез на їх реалістичність
2) Якщо необхідно, надай покращення існуючих гіпотез
3) Якщо необхідно, надай інші гіпотези
4) результати аналізу збережи у файлі .tasks\marker_matrix_results.md
5) створи python-програму, яка:
- на вхід отримує ID маркерів у форматах:
    - діапазон, наприклад, 0-100
    - перелік значень, наприклад, 0,1,2,...
- для кожного маркеру розраховує функцію оцінки якості
- надає перелік ID маркерів у порядку убування значення функції



